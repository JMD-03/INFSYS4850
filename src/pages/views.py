from django.http import HttpResponse
from django.shortcuts import render
from django.contrib.auth import views as auth_views

# Create your views here.


# def signon_view(auth_views):

# return render("signon.html", {})
